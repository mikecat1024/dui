repos:
  - repo: local
    hooks:
      - id: oxfmt
        name: oxfmt
        entry: pnpm oxfmt .
        language: system
        pass_filenames: false
        always_run: true
        priority: 0
      - id: oxlint
        name: oxlint
        entry: pnpm oxlint . --type-aware --fix
        language: system
        pass_filenames: false
        always_run: true
        priority: 10
      - id: tsgo
        name: tsgo
        entry: pnpm tsgo -b --noEmit
        language: system
        pass_filenames: false
        always_run: true
        priority: 10
